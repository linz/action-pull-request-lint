name: test

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6.0.3
        with:
          fetch-depth: 0

      - name: Use Node.js
        uses: actions/setup-node@v6.4.0

      - name: Test
        run: node --test
